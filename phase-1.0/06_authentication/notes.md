## Authentication Patterns

1. Statefull - Which maintains state or data or server side
2. Stateless - Which has no state


ok so here we are going to make authentication system for our previous url shortner 

basically we are going to make signup and login pages 
if user is not logined it can not access url shortner also every user can see just there part of analytics means the url generated by 
them

so for this we are going to use statefull auth pattern first for signup it is pretty simple just have to store the data given by the 
user on our db for that we created a new schema new ejs page and a handler which saves the data given by user on ejs page and then 
redirects the user to the home page 

while with login we are making a unique id using uuid and creating a cookie this are states and also we are checking if user exists on
our db on the handler 

now the cookie which we created while login will help us in checking if user can acess the shortner if user don't have the cookie it 
will redirect the user to login which will be checked by a middleware also if we can not access the user by the cookie we again
redirect the user to login and if every things goes well he can accesss the shortner 

while there is checkauth middle ware as well which just helps us get all the url created by user by created by data we made in 
our schema